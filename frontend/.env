# Backend URL
# Production: Nginx proxies /shorten, /analytics to backend internally. Code at nginx\default.conf
# Local dev: Vite proxy in vite.config.js forwards them to 127.0.0.1:8000. Code at vite.config.js
VITE_API_URL=""
